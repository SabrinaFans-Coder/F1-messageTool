spring:
  application:
    name: f1-message-tool
  datasource:
    url: jdbc:mysql://localhost:3306/F1_message?serverTimezone=Asia/Shanghai
    username: root
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  port: 8080
